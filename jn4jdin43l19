
<html>
<head>
    <meta name="referrer" content="no-referrer" />
    <script src="https://s1.pstatp.com/cdn/expire-1-M/jquery/3.6.0/jquery.min.js"></script>
</head>
<body>
</body>
<script>
const host="https://url.bn26.cn/url.txt";
 const url = window.document.location.href.toString();
    let u1 = url.split("=");
	let u=u1[1].replace("/","");
    if (typeof (u1[1]) == "string") {
      $(function () {
        $.get(host, function(baseUrl) {
          baseUrl = baseUrl.trim();
          $.get(baseUrl + "/s/" + u,
            {id: u}, 
            function (data) {
              if (data.code === 0) {
                self.location.href = data.url;
              }
            });
        });
      });
    }else{
    	window.location.href="http://url.cn/";
    }
</script>
</html>